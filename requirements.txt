fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
pydantic==2.9.2
pydantic-settings==2.5.2
shapely==2.0.6
geopandas==1.0.1
chromadb==0.5.15
sentence-transformers==3.1.1
scikit-learn==1.5.2
pandas==2.2.3
mistralai==1.1.0
python-dotenv==1.0.1
unstructured[md,pdf]==0.20.2
tenacity==9.0.0

# --- Qdrant migration (push_to_qdrant.py) ---
qdrant-client==1.12.1
fastembed==0.7.4  # 0.4.2 doesn't exist on PyPI for py3.13 (oldest available there is 0.5.0) — see README §2.2's "if a pin fails" note, same situation

# --- Phase B: DL crop classifier ---
torch==2.6.0  # 2.4.1 has no wheel for Python 3.13+ — bumped to the oldest version that does; TempCNN only uses stable Conv1d/BatchNorm1d/Linear/LogSoftmax layers, so version sensitivity here is low
breizhcrops==0.0.4.1  # only used for the TempCNN architecture class — no network calls to their S3 bucket happen at import/inference time
numpy==2.1.1