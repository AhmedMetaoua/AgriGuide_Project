# ==============================================================================
# REQUIRED API KEYS & CREDENTIALS
# ==============================================================================

# Mistral AI API Key
# Required for Stage 1/2 synthesis (services/synthesis_service.py) and
# for better-quality embeddings in rag_service.py.
# Source: Get a free key at https://console.mistral.ai (La Plateforme free tier)
MISTRAL_API_KEY=your_mistral_api_key_here

# Copernicus Data Space Ecosystem / Sentinel Hub Credentials
# Required for satellite imagery access.
# Source: Create an account and client credentials at https://shapp.sentinel-hub.com/dashboard/
COPERNICUS_CLIENT_ID=your_copernicus_client_id_here
COPERNICUS_CLIENT_SECRET=your_copernicus_client_secret_here

# Qdrant Vector Database
# Required for RAG vector search and index storage.
# Source: Get cluster URL and API key at https://cloud.qdrant.io/
QDRANT_URL=https://your-cluster-id.cloud.qdrant.io
QDRANT_API_KEY=your_qdrant_api_key_here

# ==============================================================================
# OPTIONAL CONFIGURATION (Defaults configured in config.py)
# ==============================================================================
# Override these only if you want a different model or self-hosted setup.

# MISTRAL_MODEL=mistral-small-latest
# MISTRAL_EMBED_MODEL=mistral-embed

# Local embedding fallback (used automatically if MISTRAL_API_KEY is
# missing or a request fails) — no key needed, runs locally.
# LOCAL_EMBED_MODEL=intfloat/multilingual-e5-base

# CHROMA_PERSIST_DIR=./chroma_store
# CHROMA_COLLECTION=