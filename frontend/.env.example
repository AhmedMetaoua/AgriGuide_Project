# URL of the agri-advisor-parcelle FastAPI backend (uvicorn main:app --reload --port 8000).
# The merged /agriculture page (map + advisor + chatbot) talks to it directly.
VITE_API_BASE_URL=http://127.0.0.1:8000
